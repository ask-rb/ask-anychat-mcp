# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# Both are consumed from the working copies until they are released.
gem 'ask-anychat', path: '../ask-anychat'
gem 'ask-mcp', path: '../ask-mcp'

group :test do
  gem 'minitest', '~> 5.25'
  gem 'rake', '~> 13.0'
end
